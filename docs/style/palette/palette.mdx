---
sidebar_label: 'Palette'
sidebar_position: 1
title: 'Palette'
description: 'Набор системных компонентов'
---

import CardSource from '!!raw-loader!../components/Card';

import { Card } from '../components/Card';

import { ColorMatrix, Opacities, Color, Effect } from './components';
import {
  PALETTE,
  COLOR_MATRIX,
  DOT_POSITIONS_MATRIX,
  INDIGO,
  PURPLE,
  BLUE,
  GREEN,
  YELLOW,
  RED,
  DOT_POSITIONS_ROW,
} from './constants';

import palletteImage from './assets/palette.png';

<headermeta
  title='Palette'
  description='Раздел включает в себя, набор системных компонентов, используемых для построения базовых компонентов дизайн-системы. Отношения между цветами и их свойствами для создания масштабируемой основы для цвета.'
  designer='Олег Астахов'
  developer='Кирилл Артуров'
  versions={['1.0', '1.1']}
  actualVersion='1.0'
  status='Work in progress'
  json='JSON'
  sass='SASS'
  bitbucketLink='https://google.com'
  figmaLink='https://google.com'
  jiraLink='https://google.com'
  headerImage={palletteImage}
/>

## Basic color

Основные цвета в X.Y.Z для набора стилей определеяющих наполненость компонентов и фирменного стиля

<docviewport codeBlock={CardSource}>
  <examples cols={2} mb={32}>
    {PALETTE.slice(0, 4).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} valueComp={<Color style={card.valueStyle} />} />
      </example>
    ))}
  </examples>
  <examples cols={4}>
    {PALETTE.slice(4).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} valueComp={<Color style={card.valueStyle} />} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Effect

Тональный переход через черный и белый слой, cтандартное значение в дизайн системе X.Y.Z имеет шаг прозрачности в 4%, но может варироваться при необходимости создания своего шага в определении тонального перехода под проект

<docviewport>
  <examples cols={1}>
    <example colorMode='dark' style={{ backgroundColor: 'var(--color-neutral-500)' }}>
      <Card
        title='white'
        colorMode='dark'
        subtitles={['color name: neutral-100', 'HEX: #ffffff']}
        valueComp={<Opacities color='#ffffff' />}
      />
    </example>
    <example colorMode='dark' style={{ backgroundColor: 'var(--color-neutral-500)' }}>
      <Card
        title='white overlay'
        colorMode='dark'
        subtitles={['color name: neutral-100', 'HEX: #ffffff']}
        valueComp={<Opacities color='#ffffff' blendMode='overlay' />}
      />
    </example>
    <example colorMode='dark' style={{ backgroundColor: 'var(--color-neutral-500)' }}>
      <Card
        title='black'
        colorMode='dark'
        subtitles={['color name: neutral-700', 'HEX: #0a0a0a']}
        valueComp={<Opacities color='#0a0a0a' />}
      />
    </example>
    <example colorMode='dark' style={{ backgroundColor: 'var(--color-neutral-500)' }}>
      <Card
        title='black overlay'
        colorMode='dark'
        subtitles={['color name: neutral-700', 'HEX: #0a0a0a']}
        valueComp={<Opacities color='#0a0a0a' blendMode='overlay' />}
      />
    </example>
  </examples>
</docviewport>

<divider />

## Sample

Компонент содержащий слои, которые накладываются поверх основного цвета, и дает возможность настроить тональный спект основного цвета

<docviewport>
  <Effect />
</docviewport>

<divider />

## Sample combination

Совмещенная палитра, базовых цветов и наложение эффектов, для выбора цветовых оттенков

<docviewport>
  <ColorMatrix colorMatrix={COLOR_MATRIX} dotsPositions={DOT_POSITIONS_MATRIX} type='matrix' />
  <ColorMatrix colorMatrix={INDIGO} dotsPositions={DOT_POSITIONS_ROW} />
  <ColorMatrix colorMatrix={PURPLE} dotsPositions={DOT_POSITIONS_ROW} />
  <ColorMatrix colorMatrix={BLUE} dotsPositions={DOT_POSITIONS_ROW} />
  <ColorMatrix colorMatrix={GREEN} dotsPositions={DOT_POSITIONS_ROW} />
  <ColorMatrix colorMatrix={YELLOW} dotsPositions={DOT_POSITIONS_ROW} />
  <ColorMatrix colorMatrix={RED} dotsPositions={DOT_POSITIONS_ROW} mb={0} />
</docviewport>

<divider marginTop={128} marginBottom={0} />
