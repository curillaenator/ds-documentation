---
sidebar_label: 'Color'
sidebar_position: 2
title: 'Color'
description: 'Библиотека для работы с цветом'
---

import headerImage from './assets/header.png';
import { Card } from '../components/Card';
import { COLORS } from './constants';

<headermeta
  title='Color'
  description='Общая подключаемая библиотека для работы с цветом'
  designer='Олег Астахов'
  developer='Кирилл Артуров'
  version='1.0'
  status='Work in progress'
  json='JSON'
  sass='SASS'
  bitbucketLink='https://google.com'
  figmaLink='https://google.com'
  jiraLink='https://google.com'
  headerImage={headerImage}
/>

## Neutral

<docviewport>
  <examples cols={3} mb={32}>
    {COLORS.neutral.slice(0, 3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
  <examples cols={4}>
    {COLORS.neutral.slice(3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Accent-1

<docviewport>
  <examples cols={3} mb={32}>
    {COLORS['accent-1'].slice(0, 3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
  <examples cols={4}>
    {COLORS['accent-1'].slice(3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Accent-2

<docviewport>
  <examples cols={3} mb={32}>
    {COLORS['accent-2'].slice(0, 3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
  <examples cols={4}>
    {COLORS['accent-2'].slice(3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Informative

<docviewport>
  <examples cols={3} mb={32}>
    {COLORS['informative'].slice(0, 3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
  <examples cols={4}>
    {COLORS['informative'].slice(3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Positive

<docviewport>
  <examples cols={3} mb={32}>
    {COLORS['positive'].slice(0, 3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
  <examples cols={4}>
    {COLORS['positive'].slice(3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Attention

<docviewport>
  <examples cols={3} mb={32}>
    {COLORS['attention'].slice(0, 3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
  <examples cols={4}>
    {COLORS['attention'].slice(3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Negative

<docviewport>
  <examples cols={3} mb={32}>
    {COLORS['negative'].slice(0, 3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
  <examples cols={4}>
    {COLORS['negative'].slice(3).map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider marginTop={128} marginBottom={0} />
