---
sidebar_label: 'Border'
sidebar_position: 3
title: 'Border'
description: 'Значения по толщине обводки, их скруглений, и их стиль оформления'
---

import headerImage from './assets/headerImage.png';

import {Card} from '../components/Card'
import {BORDER_WIDTHS, BORDER_RADIUSES, BORDER_STYLES} from './constants'


<headermeta
  title='Border'
  description='Включает в себя, значение по толщине обводки, их скруглений, и их стиль оформления'
  designer='Олег Астахов'
  developer='Кирилл Артуров'
  version='1.0'
  status='Work in progress'
  json='JSON'
  sass='SASS'
  bitbucketLink='https://google.com'
  figmaLink='https://google.com'
  jiraLink='https://google.com'
  headerImage={headerImage}
/>

## Border width

Толщина обводки

<docviewport>
  <examples cols={3}>
     {BORDER_WIDTHS.map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
</docviewport>

<divider />

## Border radius

Скругление обводки

<docviewport>
  <examples cols={3}>
    {BORDER_RADIUSES.map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
</docviewport>


<divider />

## Border style

Скругление обводки

<docviewport>
  <examples cols={2}>
    {BORDER_STYLES.map((card) => (
      <example key={card.name} exampleCode={card.exampleCode}>
        <Card {...card} />
      </example>
    ))}
  </examples>
</docviewport>


<divider marginTop={128} marginBottom={0} />
